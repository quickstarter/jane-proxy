config:
  target:  "http://localhost:3008"
  phases:
    - duration: 240
      arrivalRate: 10
  variables:
    id:
      - 1
  # processor: "./artilleryScripts.js"
scenarios:
  - flow:
    - loop:
      - get:
          url: "/api/updates/{{ $randomNumber(1,99) }}"
      count: 120
      